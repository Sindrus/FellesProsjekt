package util;


/**
 * This enum class defines the type of change the Gui Listener recieves.
 * @author jorgen
 *
 */

public enum ChangeType {
	LOGIN,
	LOGOUT,
	MEETING,
	CREATE,
	CANCEL,
	BACK,
	CREATEMEETING,
	DELETE,
	SAVECHANGE, NEWAPP, CALENDAR, PREVWEEK, NEXTWEEK,
}